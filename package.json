{
  "name": "@bandress-dumbasses/lame",
  "version": "0.0.4",
  "main": "index.js",
  "repository": "https://github.com/brainthinks/lame-test.git",
  "license": "MIT",
  "scripts": {
    "build": "rm -rf dist && mkdir dist && touch dist/npmpublishkeep.txt",
    "prepublish": "npm run build",
    "postpublish": "rm -rf dist"
  }
}
